# 桥梁病害CAD标注系统开发

## 开发日期
2026-03-20

## 项目概述
根据桥梁病害Excel记录自动生成CAD病害展示图的工具。

## 已创建文件

### 核心模块
1. `bridge_disease_parser.py` - 数据解析模块
2. `bridge_disease_cad.py` - CAD操作模块（ezdxf）
3. `bridge_disease_coords.py` - 坐标转换模块
4. `bridge_disease_main.py` - 主程序

## 2026-03-20 改进

### 标题位置修正
- 路线名称：第2个方框 (31773.0, -3858.5)
- 桥梁名称：第4个方框 (31875.0, -3858.5)
- 孔次：右下方 (31920.0, -3855.0)
- 梁号：右下方 (31920.0, -3865.0)

### 病害坐标系（梁底）
- 原点X: 横向标尺0的X坐标 = 31657.2
- 上方T梁原点Y: 纵向标尺1第一个出现的点 = -3901.3 (1-1号)
- 下方T梁原点Y: 纵向标尺1第3个出现的点 = -4001.7 (1-2号)
- X轴方向: 从左到右
- Y轴方向: 从上到下
- 1m = 10 CAD单位

### 裂缝绘制（2026-03-21 更正）
- 所有裂缝（含纵向/横向）= 水平线，从 (x_start, y) 到 (x_end, y)
- "纵向裂缝"在此项目中也是水平线，用户确认
- 裂缝 CAD 坐标 = (原点X + x*10, 原点Y - y*10)
- **手绘风格**：在两点之间添加5个中间点，每个点带±0.3以内的随机Y偏移，模拟手绘效果
  - 使用 lwpolyline 而非 LINE
  - 固定随机种子(42)保证可复现

### 网状裂缝绘制（2026-03-21 新增）
- 矩形区域由 (x_start, y_start) 到 (x_end, y_end) 两个对角确定
- 从 templates/病害图例/网状裂缝.dxf 提取 SPLINE 控制点作为折线平铺
- add_mesh_crack() 方法在 EZDXFOperator 中实现

### 构件编号解析（2026-03-21 新增/更新）
- parse_component_id(comp_id, part_name) 函数新增至 bridge_disease_parser.py
- T梁格式：'1-1号' → 孔号=1, 梁号=1；纯数字如'3号' → 只有孔号；无法识别的返回 None
- 柱墩（单柱墩/双柱墩，判断依据：part_name含'柱墩'或'下部'）：只解析盖梁号，'1号' → cap_beam_no=1
- parse_excel 返回的每条记录中新增 '孔号'、'梁号'、'盖梁号' 字段

### Bug修复（2026-03-21）
- bridge_disease_parser.py: y单值正则 `y=([\d.]+)m[,，]` 中逗号可选，修为 `(?:[,，]|$|\s)`
  原因：split('，') 后的 part 末尾无逗号导致 y 解析为 0

### read_tbeam_template.py 更新（2026-03-21 下午）- 用户直接提供固定值

**模板坐标系原点**:
- 横向标尺0 X坐标: 83.8
- 纵向标尺1 Y坐标:
  - 上方T梁: [274, 244]
  - 下方T梁: [132, 160]
- 纵向标尺2.25 Y坐标:
  - 上方T梁: [262.3, 256.5]
  - 下方T梁: [144.2, 150]

**各部件原点坐标（用户提供的固定值）**:
| 部件 | 上方T梁 | 下方T梁 | Y方向 |
|------|---------|---------|-------|
| 梁底 | (83.8, 274) | (83.8, 160) | 从上到下 |
| 左翼缘板 | (83.8, 262.3) | (83.8, 150) | 从下到上 |
| 右翼缘板 | (83.8, 256.5) | (83.8, 144.2) | 从上到下 |
| 左腹板 | (83.8, 262.3) | (83.8, 150) | 从下到上 |
| 右腹板 | (83.8, 256.5) | (83.8, 144.2) | 从上到下 |

**部件标注位置**:
- a-a: (58.2, 314.3) - 下方T梁横断面
- b-b: (56.6, 213.0) - 上方T梁横断面
- LLL: (178.9, 354.7) - 路线名称标注后
- QQQ: (297.1, 356.1) - 桥梁名称标注后
- KKK: (441.5, 360.8) - 孔次标注后
- HHH: (440.8, 348.0) - 梁号标注后

## 问题解决
- OK 标题位置修正
- OK 所有裂缝按 x_start→x_end 水平线绘制
- OK 网状裂缝按矩形区域平铺图例纹理
- OK y坐标解析 bug 修复
- OK 梁底坐标系精确定位
- 待验证 标题字段是否显示在正确方框中（需CAD软件打开查看）
- OK 双柱墩病害类型解析：添加缺少的病害类型（水平裂缝、露筋、破损、孔洞空洞）
- OK 添加具体部件字段解析（梁底、左翼缘板、右翼缘板等）
- OK 添加病害图例字段解析，输出对应 dxf 文件名
- OK 找到左翼缘板原点坐标（动态计算方式）
- OK 输出标题信息到对应方框的位置

### 不绘制的病害类型
- **泛白**：不绘制图例也不显示标注（直接跳过）

### 病害标注样式（2026-03-21）
- 第一段：从病害区域引出，45度角，长度8
- 第二段：水平线，长度根据文字长度决定

**文字位置**：
- 病害名（如"剥落"）：在第二段水平线上方，紧贴线条
- 数值（如"S=0.02m²"、"L=4.00m"）：在第二段水平线下方
- 字体大小：2.5
- 颜色：7

**示例**（左翼缘板剥落 x=0.4-0.6m, y=0.5-0.6m）：
- 起点：(89.8, 267.8) - 区域右上角
- 折点：(95.5, 273.5) - 45度角，长度8
- 终点：(107.5, 273.5) - 水平线，长度约12
- "剥落"位置：(97.5, 274.8) - 线上方
- "S=0.02m²"位置：(97.5, 269.8) - 线下方

**裂缝标注**：
- 折线引线样式同剥落
- 第二段线上方：病害名 + 长度，如"纵向裂缝 L=4.00m"
- 第二段线下方：宽度，如"W=0.10mm"

**网状裂缝标注**：
- 病害区域用波浪线填充（波长4，波幅0.3，线间距1）
- 折线引线样式同剥落
- 第二段线上方：病害名，如"网状裂缝"
- 第二段线下方：面积，如"S=9.00m²"

**裂缝群绘制（N=数字多条裂缝）**：✅
- 所有含 N=数字 的裂缝病害，都使用手绘风格绘制多条裂缝线
- 裂缝间距：线间距 1 CAD单位，线条位于区域中线（+0.5偏移）
- 手绘风格：
  - 使用正弦波模拟自然曲线：`math.sin((x - x_start) / 3) * 0.15`
  - 添加微小随机偏移：`random.uniform(-0.1, 0.1)`
  - 步进0.8生成点，最后补终点
- 标注样式：折线引线 + 第二段线上方`病害名 L总=xxxm N=数字条`，下方`间距xxxm`
  - L总从Excel读取 L总=xxxm 字段
  - 间距从Excel读取 间距xxxm 字段

**剥落露筋标注**：
- 病害区域先画白色剥落图（来自"剥落、掉角.dxf"）
- 再叠加红色虚线格栅（3条横线 + 3条竖线）
- 折线引线样式同剥落
- 第二段线上方：病害名，如"剥落露筋"
- 第二段线下方：面积，如"S=0.02m²"

## 2026-03-21 傍晚 - 上部病害批量处理程序

### 新增文件
- `bridge_disease_main_upper.py` - 主程序，处理上部病害
- `bridge_disease_main_lower.py` - 下部病害处理程序（双柱墩、桥台）

### 下部病害处理（2026-03-21 晚上）
**处理范围**：
- 双柱墩病害：11个构件（1号、2号、3号、4号、5号、6号、8号、10号、11号、12号、4-1号）
- 桥台病害：1个构件（13号）

**模板配置**：
- 双柱墩：`templates/构件/双柱墩.dxf`
- 桥台：`templates/构件/不带台身桥台.dxf`

**病害区域配置**（双柱墩模板坐标系）：
- 小桩号面：origin=(56.4, 139.5), width=45.5, height=11.2
- 大桩号面：origin=(159.9, 139.5), width=58.6, height=11.2
- 右侧面：origin=(121.9, 139.5), width=38.0, height=11.2
- 左侧齿块：origin=(48.0, 105.3)
- 右侧齿块：origin=(156.3, 93.1)

**标题占位符**：
- LLL → 路线名称
- QQQ → 桥梁名称
- GGG → 盖梁号（如"1号盖梁"）

**输出文件**：
- 下部病害_1号.dxf ~ 下部病害_12号.dxf（11个）
- 桥台病害_13号.dxf（1个）

### 处理流程
1. 读取Excel，解析上部（40mT梁）的病害数据
2. 将构件两两配对：
   - 11个构件 → 6页（5对 + 1个单独）
   - 配对规则：按顺序每两个一组
3. 对每对构件：
   - 修改标题：LLL→路线名称，QQQ→桥梁名称，KKK→孔次，HHH→梁号
   - 修改横断面：a-a→第一个构件，b-b→第二个构件
   - 绘制病害标注到对应构件
   - **单独构件时**（len(pair)==1）：删除b-b区域及标尺线
     - 删除范围: `(28, 217)` 到 `(503, 78)` 矩形区域
     - 只删除该范围内的所有实体
4. 生成最终合并文件，所有图从上到下间隔50

### 生成文件
- `output_pages/上部病害_第1-6页.dxf` - 分页文件
- `上部病害_合并结果.dxf` - 合并后的完整文件

### 病害绘制方法
- **剥落**：从模板 `templates/病害图例/剥落、掉角.dxf` 的 `adfsd` 块提取，包含7条短线（锯齿边缘）+ 18点多边形（主体）
- **剥落露筋**：剥落图形 + 红色虚线格栅
- **裂缝**：手绘风格lwpolyline（正弦波 + 随机偏移）
- **裂缝群**：多条手绘风格裂缝，间距1
- **网状裂缝**：波浪线填充（波长4，波幅0.3，线间距1）

### Bug修复（2026-03-21 傍晚）
1. MTEXT文字替换：添加对MTEXT类型的处理（使用`entity.text`而非`entity.dxf.text`）
2. 网状裂缝Y轴：修复波浪线数量计算错误，使用`abs()`取绝对值，`max()`取Y较大值作为顶部

### DXF版本注意
- 模板文件版本：AC1032 (AutoCAD 2020)
- 必须使用 `ezdxf.new(dxfversion='AC1032')` 创建兼容文档
- LWPOLYLINE需要DXF R2000以上版本

### 部件原点配置
```python
BEAM_ORIGINS = {
    'upper': {
        '梁底': (83.8, 274),
        '左翼缘板': (83.8, 262.3),
        '右翼缘板': (83.8, 256.5),
        '左腹板': (83.8, 262.3),
        '右腹板': (83.8, 256.5),
        '马蹄左侧面': (83.8, 244),  # 估计值
    },
    'lower': {
        '梁底': (83.8, 160),
        '左翼缘板': (83.8, 150),
        '右翼缘板': (83.8, 144.2),
        '左腹板': (83.8, 150),
        '右腹板': (83.8, 144.2),
        '马蹄左侧面': (83.8, 132),  # 估计值
    }
}
```

### 坐标转换
- CAD X = 原点X + x * 10
- CAD Y = 原点Y - y * 10（梁底坐标系从上到下）

### 剥落图例提取方法（2026-03-21 晚上）
从 `templates/病害图例/剥落、掉角.dxf` 的 `adfsd` 块提取：
- 7条LINE：模拟不规则边缘的锯齿
- 1个18点LWPOLYLINE：不规则多边形主体
- 绘制时缩放对齐到病害区域

### Bug修复（2026-03-21 晚上）
**bridge_disease_parser.py**：`parse_excel` 函数未将 `length`、`width`、`area`、`count` 字段合并到返回记录中
- 修复：添加这4个字段到 part_data.append 字典中

### Bug修复（2026-03-21 晚）- 病害构件分配
**问题**：所有病害都被画在同一个T梁上，没有根据构件编号分配到正确的T梁
- 1-1号病害画在了上方T梁（正确）
- 1-2号病害也画在了上方T梁（错误，应该在下方T梁）

**修复**：
1. 添加 `get_beam_level_from_comp_id(comp_id)` 函数：根据梁号判断T梁位置
   - 梁号奇数（如1-1号、1-3号）→ 上方T梁
   - 梁号偶数（如1-2号、1-4号）→ 下方T梁
2. 修改 `process_disease_record` 函数：添加 `comp_id` 参数，根据构件编号选择正确的坐标系

### 模板文字替换方法
**重要**：模板中的占位符（如 LLL、QQQ、KKK、HHH、a-a、b-b）可能是 **TEXT** 或 **MTEXT** 类型：
- **TEXT** 类型：使用 `entity.dxf.text` 属性
- **MTEXT** 类型：使用 `entity.text` 属性（无 .dxf）
- MTEXT 可能带格式代码，如 `{\C0;LLL}`

**替换函数**：
```python
def update_text_in_msp(msp, old_text: str, new_text: str):
    for entity in msp:
        if entity.dxftype() == 'TEXT':
            if old_text in entity.dxf.text:
                entity.dxf.text = entity.dxf.text.replace(old_text, new_text)
        elif entity.dxftype() == 'MTEXT':
            content = entity.text
            if old_text in content:
                entity.text = content.replace(old_text, new_text)
```

### 2026-03-21 晚上 - T梁标注方向修正

**问题**：病害标注有时会超出T梁范围（x>483）

**修复**：
- 标注x坐标范围限制在83~483之间
- 如果向右画会超出483，自动改为向左画
- `draw_polyline_leader()` 和 `draw_disease_label()` 增加 `go_left` 参数
- `process_disease_record()` 计算右边界判断是否需要向左画

**T梁坐标系（左面/右面）**：
- 左面（左翼缘板、左腹板）：Y轴正向向上
- 右面（右翼缘板、右腹板）：Y轴负向向下（原逻辑）

**不绘制部件**：马蹄左侧面、马蹄右侧面、所有齿块不绘制病害和标注

### Bug修复（2026-03-22）- 病害类型解析顺序错误
**问题**：`bridge_disease_parser.py` 中 disease_types 列表里"剥落"排在"剥落露筋"之前，导致"剥落露筋"被错误解析为"剥落"
**修复**：将更具体/更长的关键词排在前面，顺序改为：剥落露筋、漏筋、锈胀露筋、露筋、剥落掉角、剥落

### 剥落露筋绘制（2026-03-22 最终确定，用户确认）✅
- = **剥落不规则图形（来自 `剥落、掉角.dxf` 的 `adfsd` 块）** + **叠加红色虚线格栅（3条横线+3条竖线）**
- 实现方式：先调用 `draw_peel_off(return_bbox=True)`，取得实际绘制的边界框，再用该范围叠加红色虚线
- 关键：剥落图形用 `min(scale_x, scale_y)` 保持比例，实际区域比传入矩形小；格栅范围必须用实际 bbox 而非原始矩形，否则线会露出图形外
- `draw_peel_off()` 增加 `return_bbox` 参数，返回 (ax1, ay1, ax2, ay2)
- 注意：`load_rebar_geometry()` 函数保留但不再使用

## 2026-03-22 蜂窝麻面图例修复
**问题**：蜂窝麻面图例中的蓝白点（DOTS pattern）没有正确显示
**原因**：之前代码使用`add_hatch`创建新HATCH时，ezdxf将DOTS pattern转为SOLID

**解决方案**：使用`entity.copy()`和`Matrix44.transform()`复制原始HATCH实体
```python
new_entity = entity.copy()
transform = Matrix44.scale(scale, scale, 1) @ Matrix44.translate(offset_x, offset_y, 0)
new_entity.transform(transform)
msp.add_entity(new_entity)
```
- 关键：必须用`copy()`方法保留原始HATCH的DOTS pattern属性
- 不能再用`add_hatch()`创建新实体，那样会丢失pattern

**文件**：`bridge_disease_main_lower.py` 的 `draw_honeycomb()` 函数

## T梁原点坐标（2026-03-23 更新）

### 上方T梁（1-1号，奇数梁）
| 部件 | 原点坐标 (X, Y) | Y方向 |
|------|-----------------|-------|
| 梁底 | (84, 262) | 从上到下 |
| 左翼缘板 | (84, 284.4) | 从下到上 |
| 右翼缘板 | (84, 234.4) | 从上到下 |
| 左腹板 | (84, 284.4) | 从上到下 |
| 右腹板 | (84, 234.4) | 从下到上 |

### 下方T梁（1-2号，偶数梁）
| 部件 | 原点坐标 (X, Y) | Y方向 |
|------|-----------------|-------|
| 梁底 | (83.8, 150) | 从上到下 |
| 左翼缘板 | (83.8, 172) | 从下到上 |
| 右翼缘板 | (83.8, 122) | 从上到下 |
| 左腹板 | (83.8, 172) | 从上到下 |
| 右腹板 | (83.8, 122) | 从下到上 |

### 坐标转换规则
- X方向：统一从左到右，`CAD_X = origin_X + x * 10`
- Y方向：根据部件Y方向决定
  - `从上到下`：`CAD_Y = origin_Y - y * 10`（Y值减小）
  - `从下到上`：`CAD_Y = origin_Y + y * 10`（Y值增大）

## 蜂窝麻面平铺逻辑（2026-03-23 最终确认）✅
- **图例文件**：`templates/病害图例/蜂窝麻面.dxf`，extmin≈(0.1, -0.14)，原始尺寸≈4.6×3.3
- **缩放**：scale=2.0（放大2倍），scaled_w≈9.2，scaled_h≈6.7（不放大看不出蜂窝）
- **变换矩阵**（直接构造，不用矩阵链）：
  - `dx = tile_x - src_min_x * scale`
  - `dy = tile_y - src_min_y * scale`
  - `transform = Matrix44([scale,0,0,0, 0,scale,0,0, 0,0,1,0, dx,dy,0,1])`
  - 等价于 p_new = p * scale + (tile - src_min * scale)
- **对齐**：图例左上角（src_min_x, src_min_y）对齐矩形左上角（cad_x1, cad_y1）
  - start_x=cad_x1（矩形左边界），start_y=cad_y1（矩形上边界，Y较小）
  - tile_y = cad_y1 + row * scaled_h（向下增加）
- **平铺数量**：用 floor（不超出边界），`n_cols=floor(dst_w/scaled_w)`，`n_rows=floor(dst_h/scaled_h)`
- **不超出矩形**：每个方向只画完整格子，宁可留白也不超边
- **标注**：折线引线（45度+水平线）+ 上方病害名 + 下方面积 S=xx m²

## 2026-03-22 更新

### 单独构件页面删除范围修正
**删除范围**: `(28, 217)` 到 `(503, 78)` 矩形区域
- 删除 b-b 文字及下方T梁区域（横向标尺、纵向标尺）

### 病害过滤
- **泛白**：不绘制图例也不显示标注（直接跳过）

### 标题文字字号调整
- 路线名称、桥梁名称、孔次：字号 6
- 梁号：字号 6.5

### 合并文件修复（copy_entity_with_offset函数）
**问题**：合并时丢失了MTEXT、SPLINE、HATCH、INSERT等实体

**修复**：添加了以下实体类型的复制支持：
- MTEXT：保留 text、char_height、rect_width、style、color
- SPLINE：使用 ezdxf.math.BSpline 重建
- HATCH：基础填充复制
- INSERT：块引用，保留 xscale、yscale、zscale、rotation

### 合并图页偏移修正
- 图框高度：365
- 图与图间距：50
- 每页偏移：`(365 + 50) * 页序号`

## 下部墩柱病害处理（2026-03-23）

### 墩柱病害绘制规则（用户确认）
- **不关注x/y坐标**：直接在墩柱矩形范围内绘制
- **病害区域**：墩柱矩形中间偏上区域，留边距
  - X方向：左右各留4 CAD单位边距
  - Y方向：取墩柱高度25%~65%的区域（偏上）

### 墩柱识别
- `墩柱号` + `柱内编号` 组合成键如 `"4-1号"` → `N-{柱内编号}`
- `location='墩柱'` 默认归类为小桩号面
- 配对逻辑：墩柱号 `"n-m号"` 的主编号 `n` 与盖梁号 `n` 匹配

### 墩柱病害样式
- **露筋** = 钢筋锈蚀或可见箍筋轮廓图例（平铺），使用 `templates/病害图例/钢筋锈蚀或可见箍筋轮廓.dxf`
- **剥落/破损** = 剥落不规则图形
- **蜂窝/麻面** = 圆点填充
- **裂缝** = 水平线 + 标注

